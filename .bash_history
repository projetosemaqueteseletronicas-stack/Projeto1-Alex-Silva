# Comandos executados DENTRO do Ubuntu do proot:
apt update && apt upgrade -y
curl -fsSL https://ollama.com/install.sh | sh
# Inicie o servidor do Ollama em segundo plano
ollama serve &
# Baixe um modelo leve e adequado para smartphones (ex: Qwen 2.5 de 1.5B)
ollama run qwen2.5:1.5b
exit
cd /caminho/para/o/seu/projeto
chmod +x gradlew
./gradlew assembleDebug
cp app/build/outputs/apk/debug/app-debug.apk /sdcard/Download/
from kivy.app import App
from kivy.uix.button import Button
class MeuApp(App):
if __name__ == "__main__":;     MeuApp().run()
pkg install python
pip install buildozer
buildozer init
mkdir -p .github/workflows
name: Build APK
on: [push]
jobs:
cd /caminho/para/o/seu/projeto
chmod +x gradlew
./gradlew assembleDebug
cp app/build/outputs/apk/debug/app-debug.apk /sdcard/Download/
from kivy.app import App
from kivy.uix.button import Button
class MeuApp(App):
if __name__ == "__main__":;     MeuApp().run()
pkg install python
pip install buildozer
buildozer init
mkdir -p .github/workflows
name: Build APK
on: [push]
jobs:
git init
git add .
git commit -m "Compilar meu App"
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git
git branch -M main
git push -u origin main
mkdir -p .github/workflows
.github/workflows/build.yml
name: Build APK
on: [push]
jobs:
cd /caminho/para/o/seu/projeto
chmod +x gradlew
./gradlew assembleDebug
cp app/build/outputs/apk/debug/app-debug.apk /sdcard/Download/
from kivy.app import App
from kivy.uix.button import Button
class MeuApp(App):
if __name__ == "__main__":;     MeuApp().run()
pkg install python
pip install buildozer
buildozer init
mkdir -p .github/workflows
name: Build APK
on: [push]
jobs:
git init
git add .
git commit -m "Compilar meu App"
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git
git branch -M main
git push -u origin main
mkdir -p .github/workflows
.github/workflows/build.yml
name: Build APK
on: [push]
jobs:
git init
git add .
git commit -m "Compilar meu App"
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git
git branch -M main
git push -u origin main
start
# Garanta que está na pasta correta do seu projeto Python
cd /caminho/para/seu/projeto
# Crie a estrutura de diretórios para o GitHub Actions
mkdir -p .github/workflows
# Crie e edite o arquivo de configuração do workflow
micro .github/workflows/build.yml
name: Build APK
on: [push]
jobs:
cd /caminho/para/o/seu/projeto
chmod +x gradlew
./gradlew assembleDebug
cp app/build/outputs/apk/debug/app-debug.apk /sdcard/Download/
from kivy.app import App
from kivy.uix.button import Button
class MeuApp(App):
if __name__ == "__main__":;     MeuApp().run()
pkg install python
pip install buildozer
buildozer init
mkdir -p .github/workflows
name: Build APK
on: [push]
jobs:
git init
git add .
git commit -m "Compilar meu App"
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git
git branch -M main
git push -u origin main
mkdir -p .github/workflows
.github/workflows/build.yml
name: Build APK
on: [push]
jobs:
git init
git add .
git commit -m "Compilar meu App"
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git
git branch -M main
git push -u origin main
start
# Garanta que está na pasta correta do seu projeto Python
cd /caminho/para/seu/projeto
# Crie a estrutura de diretórios para o GitHub Actions
mkdir -p .github/workflows
# Crie e edite o arquivo de configuração do workflow
micro .github/workflows/build.yml
name: Build APK
on: [push]
jobs:
name: Build APK
on: [push]
jobs:
